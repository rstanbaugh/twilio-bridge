[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "twilio-bridge"
version = "0.1.0"
description = "Twilio SMS bridge for OpenClaw"
requires-python = ">=3.11"
dependencies = [
    "Flask>=2.2",
    "requests>=2.28",
    "twilio>=8.0",
]
